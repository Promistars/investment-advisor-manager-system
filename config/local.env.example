# 本机可选覆盖 — 复制为 config/local.env（该文件不入库）
#
# 默认使用 conda 环境名 IAMS（与 environment.yml 一致），
# 由 scripts/load_local_env.sh 自动解析 python / npm 路径。
#
# 仅当 conda 不在默认位置时取消注释：
# CONDA_BASE=/path/to/miniconda3
#
# 可选
# IAMS_CONDA_ENV=IAMS
# IAMS_PORT=29996
